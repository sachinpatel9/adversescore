"""Agent tool boundary — placeholder.

The old single `get_adverse_score` tool tied to the five-capability composite
score has been removed as part of the PSUR consolidation rebuild (Phase 0,
see docs/PSUR_CONSOLIDATION_SCOPE.md). The new tool decomposition wrapping
drug_identity.py / fda_client.py / deduplication.py / ranking.py /
consolidation.py is proposed and built in Phase 8.
"""
